bundle:
  name: my-sql-agent

variables:
  warehouse_id:
    description: SQL Warehouse ID used by the agent
    default: "097eb506f6dfe191"

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-fbd25ea6-bf4e.cloud.databricks.com
  prod:
    mode: production
    workspace:
      host: https://dbc-fbd25ea6-bf4e.cloud.databricks.com
      root_path: /Workspace/Users/sabitpradhan4@gmail.com/.bundle/${bundle.name}/${bundle.target}
